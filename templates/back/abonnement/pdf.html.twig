<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            color: #003366; /* Bleu marine pour le texte principal */
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .header h1 {
            margin: 0;
            color: red; /* Titre en rouge */
        }
        .mentor-name {
            position: absolute;
            top: 20px;
            left: 20px;
            font-size: 18px;
            font-weight: bold;
            color: #003366; /* Bleu marine */
        }
        .table-container {
            width: 100%;
            margin-top: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .footer {
            text-align: center;
            margin-top: 40px;
            font-size: 12px;
            color: #003366; /* Bleu marine pour le texte du footer */
        }
    </style>
</head>
<body>

    <!-- Nom de la page Mentor -->
    <div class="mentor-name">Mentor</div>

    <div class="header">
        <h1>Payment List</h1>
        <p>Generated on: {{ "now"|date("Y-m-d H:i") }}</p>
    </div>

    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>Email</th>
                    <th>Offer</th>
                    <th>Payment Date</th>
                </tr>
            </thead>
            <tbody>
                {% for paiement in paiements %}
                    <tr>
                        <td>{{ paiement.user.email }}</td>
                        <td>{{ paiement.offre.nomOffre }}</td>
                        <td>{{ paiement.datePaiement|date('Y-m-d H:i') }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">No payments found.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="footer">
        <p>Thank you for using our platform.</p>
    </div>

</body>
</html>
