<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>List of Offers</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #1A237E; /* Marine Blue */
            margin: 20px;
            padding: 0;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 3px solid red;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }

        .header h1 {
            color: red; /* Red title */
            font-size: 24px;
            margin: 0;
        }

        .logo {
            width: 80px;
        }

        .mentor-name {
            position: absolute;
            top: 20px;
            left: 20px;
            font-size: 18px;
            font-weight: bold;
            color: #1A237E; /* Marine Blue */
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #1A237E; /* Marine Blue */
            color: white;
            text-transform: uppercase;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        td {
            font-size: 14px;
        }

        .footer {
            text-align: center;
            margin-top: 30px;
            font-size: 12px;
            color: grey;
        }
    </style>
</head>
<body>

    <!-- Mentor name in the top left corner -->
    <div class="mentor-name">Mentor</div>
 <br></br>
    <div class="header">
        <h1>List of Offers</h1>
    </div>

    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Price (€)</th>
                <th>Start Date</th>
                <th>End Date</th>
                <th>Description</th>
            </tr>
        </thead>
        <tbody>
            {% for offre in offres %}
                <tr>
                    <td>{{ offre.getNomOffre() }}</td>
                    <td>{{ offre.getPrix() }} €</td>
                    <td>{{ offre.getDateDebut()|date('d/m/Y') }}</td>
                    <td>{{ offre.getDateFin()|date('d/m/Y') }}</td>
                    <td>{{ offre.getDescription() }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        Generated automatically - {{ "now"|date("d/m/Y H:i") }}
    </div>

</body>
</html>
