<form action="{{ path('app_group_delete_all') }}" method="POST" onsubmit="return confirm('Voulez-vous vraiment tout supprimer ?');">
    <button type="submit" class="btn btn-danger">
        🗑️ Supprimer tous les groupes
    </button>
</form>
