{% extends 'front/baseback.html.twig' %}

{% block title %}Group{% endblock %}

{% block body %}
    <h1>Group</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group.id }}</td>
            </tr>
            <tr>
                <th>Id_group</th>
                <td>{{ group.idGroup }}</td>
            </tr>
            <tr>
                <th>Description_group</th>
                <td>{{ group.descriptionGroup }}</td>
            </tr>
            <tr>
                <th>Nom_createur</th>
                <td>{{ group.nomCreateur }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ group.statut }}</td>
            </tr>
            <tr>
                <th>Date_creation_group</th>
                <td>{{ group.dateCreationGroup ? group.dateCreationGroup|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ group.image }}</td>
            </tr>
            <tr>
                <th>Date_meet</th>
                <td>{{ group.dateMeet ? group.dateMeet|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_group_index') }}">back to list</a>

    <a href="{{ path('app_group_edit', {'id': group.id}) }}">edit</a>

    {{ include('front/group/_delete_form.html.twig') }}
{% endblock %}
