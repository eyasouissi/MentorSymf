{% extends 'back/baseback.html.twig' %}

{% block title %}Ajouter un Événement - Back Office{% endblock %}

{% block body %}
<div class="main-panel" style="margin-left: 160px; padding: 20px;">
    <div class="container mt-5">
        <h1 class="text-center mb-4">Ajouter un Événement</h1>

       

        <form action="{{ path('evenement_newback') }}" method="post" enctype="multipart/form-data">
            {{ form_start(form) }}

            <div class="form-group">
                {{ form_label(form.titreE) }}
                {{ form_widget(form.titreE, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.titreE) }}
            </div>

            <div class="form-group">
                {{ form_label(form.descriptionE) }}
                {{ form_widget(form.descriptionE, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.descriptionE) }}
            </div>

            <div class="mb-4 input-group animated-input">
                <div class="input-group-prepend">
                    <span class="input-group-text"><i class="fas fa-calendar-alt"></i></span>
                </div>
                
                {{ form_widget(form.dateDebut, {'attr': {'class': 'form-control form-control-lg', 'placeholder': 'Start Date'}}) }}
            </div>
            {% if form.dateDebut.vars.errors|length > 0 %}
                <div class="alert alert-danger p-2">{{ form_errors(form.dateDebut) }}</div>
            {% endif %}

            <!-- End Date Field -->
            <div class="mb-4 input-group animated-input">
                <div class="input-group-prepend">
                    <span class="input-group-text"><i class="fas fa-calendar-alt"></i></span>
                </div>
                {{ form_widget(form.dateFin, {'attr': {'class': 'form-control form-control-lg', 'placeholder': 'End Date'}}) }}
            </div>
            {% if form.dateFin.vars.errors|length > 0 %}
                <div class="alert alert-danger p-2">{{ form_errors(form.dateFin) }}</div>
            {% endif %}

            <div class="form-group">
                {{ form_label(form.imageE) }}
                {{ form_widget(form.imageE, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.imageE) }}
            </div>

            <!-- Announcement Title Field -->
            <div class="form-group">
                {{ form_label(form.annonce) }} {# Change 'annonce' to the correct field name #}
                {{ form_widget(form.annonce, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.annonce) }}
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">Ajouter l'Événement</button>
                <a href="{{ path('evenement_indexback') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>

            {{ form_end(form) }}
        </form>
    </div>
</div>
{% endblock %}
