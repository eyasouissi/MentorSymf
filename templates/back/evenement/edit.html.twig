{% extends 'back/baseback.html.twig' %}

{% block title %}Modifier un Événement - Back Office{% endblock %}

{% block body %}
<div class="main-panel" style="margin-left: 160px; padding: 20px;">
    <div class="container mt-5">
        <h1 class="text-center mb-4">Modifier l'Événement: {{ evenement.getTitreE() }}</h1>

        <div class="mb-3 d-flex justify-content-end">
            <a href="{{ path('evenement_newback') }}" class="btn btn-success">Ajouter un Événement</a>
        </div>

        <form action="{{ path('evenement_editback', {'id': evenement.id}) }}" method="post" enctype="multipart/form-data">
            {{ form_start(form) }}
            
            <div class="form-group">
                {{ form_label(form.titreE) }}
                {{ form_widget(form.titreE, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.titreE) }}
            </div>

            <div class="form-group">
                {{ form_label(form.descriptionE) }}
                {{ form_widget(form.descriptionE, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.descriptionE) }}
            </div>

            <div class="form-group">
                {{ form_label(form.dateDebut) }}
                {{ form_widget(form.dateDebut, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.dateDebut) }}
            </div>

            <div class="form-group">
                {{ form_label(form.dateFin) }}
                {{ form_widget(form.dateFin, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.dateFin) }}
            </div>

            <div class="form-group">
                {{ form_label(form.imageE) }}
                {{ form_widget(form.imageE, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.imageE) }}
            </div>

            <div class="form-group">
                {{ form_label(form.annonce) }} {# Add this field for 'annonce' #}
                {{ form_widget(form.annonce, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.annonce) }}
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">Mettre à jour l'Événement</button>
                <a href="{{ path('evenement_indexback') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>

            {{ form_end(form) }}
        </form>
    </div>
</div>
{% endblock %}
